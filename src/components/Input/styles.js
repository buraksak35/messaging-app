import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 15,
    width: '60%',
    paddingVertical: 8,
    paddingHorizontal: 16,
  },
});
